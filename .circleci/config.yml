version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Random pass/fail"
          command: echo "Passing this run."

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - approve-failure:
          type: approval
          requires:
            - say-hello
